apiVersion: v1
kind: Service
metadata:
  name: sonora-web
  labels:
    app.kubernetes.io/name: sonora
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sonora
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
